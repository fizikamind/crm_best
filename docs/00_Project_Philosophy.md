CRM Best is metadata-driven. Business rules are defined by configuration, not by source code.

That means:

Developers build the platform.
Administrators configure the business.

This has several benefits:

A school can configure it for education.
A hospital can configure it for healthcare.
A distributor can configure it for sales.
An NGO can configure it for outreach.

All using the same application.