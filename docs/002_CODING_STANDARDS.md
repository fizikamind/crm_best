Every feature uses the same folder structure.
No business values are hardcoded.
Every database operation goes through a repository.
Screens never access Firestore directly.
Every completed feature must have a Git commit.